export enum EmploymentEnumType {
  FULLTIME = 'fulltime',
  CONTRACT = 'contract',
}

export enum CurrencyEnumType {
  USD = 'USD',
  NGN = 'NGN',
}

export enum PayFrequencyEnumType {
  MONTHLY = 'monthly',
  WEEKLY = 'weekly',
}

export enum RoleEnumType {
  EMPLOYER = 'employer',
  EMPLOYEE = 'employee',
  ADMIN = 'admin',
}
